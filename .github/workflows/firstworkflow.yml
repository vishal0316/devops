name: First Workflow
on: workflow_dispatch
jobs: 
    first-job:
      runs-on: ubantu-latest
      steps:
        - name: Hello github action
          run: echo "hello first"
        - name: Bye github
          run: echo "bye github"
      
